import 'package:flutter/material.dart';

class NavDrawersType1 extends StatelessWidget {
  const NavDrawersType1({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Drawer(),
    );
  }
}
